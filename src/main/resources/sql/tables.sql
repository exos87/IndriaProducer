CREATE TABLE SUSERS  (
                        USER_ID int NOT NULL,
                        USER_GUID varchar(50) NOT NULL,
                        USER_NAME varchar(150) NOT NULL,
                            PRIMARY KEY (USER_ID))  ;